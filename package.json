{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "gulp make",
    "clean": "rimraf output && rimraf .pulp-cache && rimraf build",
    "examples": "gulp bundle",
    "test": "gulp test"
  },
  "devDependencies": {
    "benchmark": "^2.1.0",
    "bower": "^1.7.2",
    "gulp": "^3.9.1",
    "gulp-purescript": "^1.0.0",
    "jsdom": "^9.4.0",
    "pulp": "^9.0.0",
    "purescript": "^0.9.1",
    "purescript-psa": "^0.3.7",
    "rimraf": "^2.5.0"
  }
}
