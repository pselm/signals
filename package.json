{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "build": "pulp build && rimraf docs && pulp docs",
    "clean": "rimraf output && rimraf .pulp-cache",
    "examples": "pulp build -I examples"
  },
  "devDependencies": {
    "bower": "^1.7.2",
    "pulp": "^8.0.0",
    "rimraf": "^2.5.0"
  }
}
